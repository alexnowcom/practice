import primes

print(primes.isPrime(5))

print(primes.listPrimes(100))

## Note: We could import a specific function from the library like this
# from primes import listPrimes
## and then we could reference it directly like this:
# print(listPrimes(100))